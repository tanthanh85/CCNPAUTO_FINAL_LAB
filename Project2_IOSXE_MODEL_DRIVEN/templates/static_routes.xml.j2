<config xmlns="urn:ietf:params:xml:ns:netconf:base:1.0">
  <native xmlns="http://cisco.com/ns/yang/Cisco-IOS-XE-native">
    <ip>
      <route>
          {# TODO: Add the opening Jinja2 for statement immediately below. and complete the varibles #}
          <ip-route-interface-forwarding-list>
            <prefix></prefix>
            <mask></mask>
            <fwd-list>
              <fwd></fwd>
            </fwd-list>
          </ip-route-interface-forwarding-list>
          {# TODO: Add the Jinja2 endfor statement immediately above. #}
      </route>
    </ip>
  </native>
</config>
